<!-- {{Skill名}} の型。**形はここが持つ** ── 組み立てのコードは、値を差し込むだけである。
     差し込む場所は {{名前}} で、過不足は `lib/template.py` がその場で例外にする。
     1つの部品の中で、同じ名前を何度使ってもよい。 -->

<template data-part="page"><title>{{title}}</title><style>{{style}}</style>{{body}}</template>
