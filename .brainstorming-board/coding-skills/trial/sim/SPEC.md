# 仕様（両方に同じものを渡す）

ブレストの回答を受け取って、1件ずつ保存する。

- 1回の送信は、論点1件につき回答1つを持つ
- 回答は 論点の番号 ・ 諾否（approve / return / skip）・ 理由 を持つ
- 直前の送信と同じ内容なら、保存しない
- HTTP の POST で受け取り、保存した件数を返す

## あとで適用する仕様変更

保存先を、ファイルから SQLite へ変える。
