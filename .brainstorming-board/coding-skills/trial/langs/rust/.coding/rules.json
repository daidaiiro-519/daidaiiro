{
  "order": [
    "core_layer",
    "adapter_layer"
  ],
  "layers": {
    "core_layer": "core_layer",
    "adapter_layer": "adapter_layer"
  },
  "rules": [
    {
      "rule": "依存の向きが、内から外へ出ていない",
      "source": {
        "record": "model"
      },
      "scope": "この成果物に限る",
      "check": {
        "tool": [
          "cargo",
          "check",
          "--workspace",
          "--quiet"
        ]
      }
    }
  ]
}
