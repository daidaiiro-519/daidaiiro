# 業務イベント駆動（concrete schema）── 注入が効いているかを確かめる試験である。
#
# 依存は一方向である ── event → base。**基盤はこちらを知らない。**
[package]
name = "event"
version = "0.1.0"
edition = "2021"

[dependencies]
base = { path = "../base" }
serde_json = "1"
