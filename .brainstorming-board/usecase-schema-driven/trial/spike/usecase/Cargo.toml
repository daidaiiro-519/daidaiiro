# ユースケース駆動（concrete schema）。基盤の上へ注入される側。
#
# 依存は一方向である ── usecase → base。基盤はこちらを知らない。
[package]
name = "usecase"
version = "0.1.0"
edition = "2021"

[dependencies]
base = { path = "../base" }
serde_json = "1"
