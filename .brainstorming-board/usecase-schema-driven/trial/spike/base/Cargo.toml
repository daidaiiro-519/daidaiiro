# 基盤（abstract schema）。
#
# **ここに concrete への依存を1件も書かない。**それが「注入が効いている」ことの機械的な判定になる。
# 依存は serde と serde_json の2件に固定する ── テンプレートエンジンは入れない。
[package]
name = "base"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
