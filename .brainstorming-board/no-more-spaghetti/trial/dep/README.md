# 層の向きの検査 ── 言語に依存しない試作

`check_layers.py`（84行、標準ライブラリのみ、依存0件）。
入力は2つ ── 記録（層 → 置き場所 ・ 並び）と、参照を書く行の形（言語ごとに正規表現1〜2本）。

## 動かした結果（2026-09-23）

| 対象 | 違反なし | 違反あり |
|---|---|---|
| Python（`trial/sim/b-rules`） | 合格 ・ 終了コード0 | `domain/__init__.py:11　domain → adapters` ・ 1 |
| TypeScript | 合格 | `domain/answer.ts:3` |
| Go | 合格 | `domain/answer.go:5` |
| Rust | 合格 | `domain/mod.rs:2` |
| Java | 合格 | `domain/Answer.java:4` |

Python では `import-linter` 2.15 と**同じ行**を出した（`domain -> adapters.file_store (l.11)`）。

## 測った限界

| 種別 | 測定 |
|---|---|
| **誤検出** | 塊のコメント（`/* ... */`）の中の import を、違反として出した（1件） |
| **取りこぼし** | 動的な読み込み（`await import("../adapters/http")`）を検出しなかった |
| 正しく無視した | 文字列の中に層の名前が現れる場合（`"see ../adapters/http"`） |

行の頭が `#` ・ `//` の場合だけを飛ばしているので、塊のコメントは読んでいる。
名前の解決を実施しないので、動的な読み込みと、設定で別名を張った場合は追えない。

## 言語を足す手間

`形` の表へ1行足すだけである（正規表現1〜2本）。**処理のコードは1行も増えない。**
